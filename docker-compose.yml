version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: myfirstspringboot
    restart: unless-stopped
    environment:
      POSTGRES_DB: mysite_dev
      POSTGRES_USER: hezhuoyao
      POSTGRES_PASSWORD: hzythebest1
    ports:
      - "2015:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d:ro